# Copy to `.env` locally; set in Vercel project settings for deploy.
# Never commit real keys.

# Public Supabase project URL (Settings → API).
NUXT_PUBLIC_SUPABASE_URL=

# Server-only — service role (Settings → API). Used only by Nitro; never in client bundle.
NUXT_SUPABASE_SERVICE_ROLE_KEY=

# Server-only — Google AI (Gemini) for hybrid intake classification on POST /api/intake/analyze. Optional; keyword parser runs if unset or on failure.
NUXT_GEMINI_API_KEY=

# Server-only — Gemini model id for intake JSON classification (default gemini-2.5-flash). Use gemini-2.5-flash-lite for a cheaper/faster tier if supported for your key.
NUXT_GEMINI_MODEL=gemini-2.5-flash

# Optional — verbose JSON stage logs for POST /api/intake/analyze (server). If unset/false, only summary + compact Gemini failure lines are logged.
# INTAKE_DEBUG_LOGS=true
# NUXT_INTAKE_DEBUG_LOGS=true
